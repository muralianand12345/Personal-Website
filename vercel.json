{
    "devCommand": "uvicorn main:app --host 127.0.0.1 --port 5328",
    "builds": [
        {
            "src": "api/app/main.py",
            "use": "@vercel/python"
        }
    ],
    "routes": [
        {
            "src": "/(.*)",
            "dest": "api/app/main.py"
        }
    ]
}
